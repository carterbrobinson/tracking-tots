import 'package:flutter/material.dart';

class CustomColors {
  static const Color primary = Color(0xFF673AB7);  // ARGB format
  static const Color background = Color(0xFFB388FF); // ARGB format

  static Color lightGrey = Colors.white.withValues(alpha: 0.1);
  static Color midGrey = Colors.white.withValues(alpha: 0.2);
  static Color darkGrey = Colors.white.withValues(alpha: 0.08);
}